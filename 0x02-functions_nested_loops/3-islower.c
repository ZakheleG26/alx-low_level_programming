#include "main.h"

/**
 * _slower - Check lowercase character
 * @c: The character to be checked
 * Return: 1 lowercase character 0 everything else
 */

int _islower(int c)

{
	if (c >= 97 && c <= 122)
	{

	return (1);
	}
	return (0);
}
